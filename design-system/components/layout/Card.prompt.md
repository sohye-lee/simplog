Base surface panel — 1px border, small radius, no shadow (Ledger favors borders over elevation). Set `elevated` only for floating layers like menus.

```jsx
<Card padding="md">…</Card>
<Card padding="lg" elevated>…</Card>
```

Props: `padding` (`none`|`sm`|`md`|`lg`), `elevated`, `as`.
